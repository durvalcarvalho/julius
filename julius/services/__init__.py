"""Use cases (importing, search, catalog, export, suggestions). Orchestrate repositories and parsers.

Functions return data and never print; the CLI (or any future interface) does the formatting.
"""
