[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "julius"
version = "2.12.0"
description = "Memória pessoal de preços de mercado a partir de recibos NFC-e"
requires-python = ">=3.10"
dependencies = [
    "typer",
    "rich",
    "rapidfuzz",
]

[project.scripts]
julius = "julius.cli:app"
julius-bot = "julius.bot:main"

[project.optional-dependencies]
dev = ["pytest", "python-telegram-bot>=21", "pydantic-ai-slim[openai]==2.45.0"]
bot = ["python-telegram-bot>=21", "pydantic-ai-slim[openai]==2.45.0"]

[tool.setuptools.packages.find]
include = ["julius*"]

[tool.setuptools.package-data]
"julius.infra.migrations" = ["*.sql"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "real_ai: fala com o modelo de verdade e gasta do orçamento; só roda com --real-ai",
]
